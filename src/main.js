import * as THREE from 'three';
import './style.css';
import {createRocket} from './rocket.js';
import {SurvivalSystem,InventorySystem,QuestSystem,ShipSystem} from './systems.js';
import {GAME_DATA} from './gameData.js';
const app=document.querySelector('#app');app.innerHTML=`<div id="hud"><b>AFTEREARTH</b><span id="objective">Objective: Explore the launch facility</span><div id="stats"></div><div id="help">WASD move · Shift sprint · E interact · V camera · I inventory · M map · Click to look</div></div><div id="crosshair">+</div>`;
const scene=new THREE.Scene();scene.background=new THREE.Color(0x070b12);scene.fog=new THREE.Fog(0x070b12,35,180);const camera=new THREE.PerspectiveCamera(70,innerWidth/innerHeight,.1,500);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;app.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0x9fb9d8,0x182018,2));const sun=new THREE.DirectionalLight(0xffffff,2);sun.position.set(20,40,10);scene.add(sun);const floor=new THREE.Mesh(new THREE.PlaneGeometry(300,300),new THREE.MeshStandardMaterial({color:0x22272b,roughness:.9}));floor.rotation.x=-Math.PI/2;scene.add(floor);function box(x,y,z,sx,sy,sz,c=0x39434b){const m=new THREE.Mesh(new THREE.BoxGeometry(sx,sy,sz),new THREE.MeshStandardMaterial({color:c,metalness:.35,roughness:.7}));m.position.set(x,y,z);m.castShadow=m.receiveShadow=true;scene.add(m)}for(let i=0;i<30;i++)box((Math.random()-.5)*80,2,(Math.random()-.5)*80,3+Math.random()*8,4,3+Math.random()*8,0x26323b);box(0,5,-28,45,10,3,0x17242d);box(-22,2,-15,3,4,30,0x31404a);box(22,2,-15,3,4,30,0x31404a);createRocket(scene);
const player=new THREE.Object3D();player.position.set(0,1.7,12);scene.add(player);player.add(camera);const survival=new SurvivalSystem(),inventory=new InventorySystem(),quests=new QuestSystem(),ship=new ShipSystem();const keys={};let yaw=0,pitch=0,third=false;const objective=document.querySelector('#objective');addEventListener('keydown',e=>{keys[e.code]=true;if(e.code==='KeyV')third=!third;if(e.code==='KeyE')objective.textContent='Objective: Search the rocket control room';if(e.code==='KeyI')objective.textContent='Inventory: empty — collect resources';if(e.code==='KeyM')objective.textContent='Map: Earth unlocked · Verdantia, Cryon, Aridion locked'});addEventListener('keyup',e=>keys[e.code]=false);renderer.domElement.onclick=()=>renderer.domElement.requestPointerLock();document.onmousemove=e=>{if(document.pointerLockElement){yaw-=e.movementX*.002;pitch=Math.max(-1.4,Math.min(1.4,pitch-e.movementY*.002))}};function animate(){requestAnimationFrame(animate);const dt=.016,sprint=keys.ShiftLeft&&survival.stamina>0,speed=sprint?.18:.09,f=new THREE.Vector3(Math.sin(yaw),0,Math.cos(yaw)),r=new THREE.Vector3(Math.cos(yaw),0,-Math.sin(yaw));if(keys.KeyW)player.position.addScaledVector(f,-speed);if(keys.KeyS)player.position.addScaledVector(f,speed);if(keys.KeyA)player.position.addScaledVector(r,-speed);if(keys.KeyD)player.position.addScaledVector(r,speed);survival.update(dt,sprint);camera.position.set(0,third?3:0,third?7:0);camera.rotation.set(pitch,yaw,0,'YXZ');document.querySelector('#stats').textContent=`HP ${survival.health|0} | STAMINA ${survival.stamina|0} | FOOD ${survival.food|0} | WATER ${survival.water|0} | SHIP HULL ${ship.hull|0}`;renderer.render(scene,camera)}animate();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});