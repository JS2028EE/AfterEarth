import * as THREE from 'three';
export function createRocket(scene){const ship=new THREE.Group();const body=new THREE.Mesh(new THREE.CylinderGeometry(2.4,3.1,14,32),new THREE.MeshStandardMaterial({color:0x8d9aa5,metalness:.8,roughness:.25}));body.rotation.z=Math.PI/2;ship.add(body);const cockpit=new THREE.Mesh(new THREE.SphereGeometry(2.1,24,12,0,Math.PI*2,0,Math.PI/2),new THREE.MeshPhysicalMaterial({color:0x15354b,metalness:.2,roughness:.05,transmission:.2,transparent:true,opacity:.85}));cockpit.position.x=6;cockpit.rotation.z=-Math.PI/2;ship.add(cockpit);for(const y of[-2.5,2.5]){const wing=new THREE.Mesh(new THREE.BoxGeometry(5, .25, 4),new THREE.MeshStandardMaterial({color:0x34414c,metalness:.7}));wing.position.set(-1,y,0);ship.add(wing)}ship.position.set(0,3,-25);scene.add(ship);return ship}